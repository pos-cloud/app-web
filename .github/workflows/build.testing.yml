name: Testing
on:
  push:
    branches: [ testing ]
  workflow_dispatch:
jobs:
  build:
    runs-on: dev
    steps:
      - uses: actions/checkout@v3
      - run: npm install
      - run: npm run build:dev
      - run: rm -Rf /var/www/html/*
      - run: cp -Rf dist/* /var/www/html/